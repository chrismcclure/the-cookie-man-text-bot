{
  "name": "the-cookie-man-text-bot",
  "version": "1.0.0",
  "description": "Text bot for The Cookie Man",
  "main": "src/index.js",
  "type": "module",
  "scripts": {
    "start": "node --env-file-if-exists=.env src/index.js",
    "dev": "node --watch --env-file-if-exists=.env src/index.js",
    "test": "node --test"
  },
  "engines": {
    "node": ">=22"
  },
  "dependencies": {
    "openai": "^7.3.0",
    "twilio": "^6.0.2"
  }
}
